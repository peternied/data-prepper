lib = library(
    identifier: 'jenkins@uni-poc',
    retriever:modernSCM(
        [$class: 'GitSCMSource',
        credentialsId: '',
        remote: 'https://github.com/peternied/opensearch-build.git',
        traits: [gitBranchDiscovery()]
        ])
    )

defaultPipeline {
    echo "hello from this closure"
}